export enum ScreenNames {
	LEASE_SCREEN_NAME = "lease document",
	MASTER_PROOF_SCREEN_NAME = "lease document",
	MONTHLY_PROOF_SCREEN_NAME = "lease document",
	DISPUTE_SCREEN_NAME = "lease document"
}
